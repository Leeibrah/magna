<?php

class M_tsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('m_ts')->truncate();

		$m_ts = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('m_ts')->insert($m_ts);
	}

}
